import { Routes } from '@angular/router';
import { HomepageComponent } from './core/features/homepage/homepage.component';

export const routes: Routes = [
  {
    path: '',
    component: HomepageComponent
  }
];
